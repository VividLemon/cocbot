Add the verification wizard

I think firstMessage contains a bug if there is multiple responses.

add try catches to make sure things don't crash

some more api stuff?

Ryan needs to update the server stuffs. Look pretty

Update bot permissions. No admin

Change nickname for others isn't working